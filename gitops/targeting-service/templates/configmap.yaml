{{- if and .Values.enabled .Values.init.configMap.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.init.configMap.name }}
  annotations:
{{ toYaml .Values.init.configMap.annotations | nindent 4 }}
data:
  init.sql: |
{{ .Values.init.configMap.sql | nindent 4 }}
{{- end }}
